# ai-tutor-rag-system
An AI Tutor system using semantic search and a local LLM (TinyLlama) to provide context-aware answers. Implements a Retrieval-Augmented Generation (RAG) approach with incremental learning capability.
